# VLO docker compose configuration overlay for beta
version: '2'
services:
  vlo_solr:
    volumes:
      - solr_temp:/tmp
  vlo_web:
    ports:
      - 80:8080
volumes:
  solr_temp:
    external: false
