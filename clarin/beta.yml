# VLO docker compose configuration overlay for beta
version: '2'
services:
  vlo-solr:
    volumes:
      - solr_temp:/tmp
  vlo-web:
    ports:
      - 80:8080
volumes:
  solr_temp:
    external: false
